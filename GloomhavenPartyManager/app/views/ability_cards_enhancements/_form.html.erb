<%= form_with(model: ability_cards_enhancement, local: true) do |form| %>
  <% if ability_cards_enhancement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ability_cards_enhancement.errors.count, "error") %> prohibited this ability_cards_enhancement from being saved:</h2>

      <ul>
        <% ability_cards_enhancement.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ability_card_id %>
    <%= form.collection_select :ability_card_id, AbilityCard.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :enhancement_id %>
    <%= form.collection_select :enhancement_id, Enhancement.all, :id, :description %>
  </div>

  <div class="field">
    <%= form.label :is_top %>
    <%= form.check_box :is_top %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
